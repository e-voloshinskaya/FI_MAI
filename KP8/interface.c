#include <stdio.h>
#include <stdlib.h>

int main()
{
    LIST* test = NULL;
    create(test);
    add_node(test, 0);
    printf();
    return 0;


    //!!!!!!!!!!!!!!!!!! передаем индекс не позицию
    int idx = pos - 1;
}